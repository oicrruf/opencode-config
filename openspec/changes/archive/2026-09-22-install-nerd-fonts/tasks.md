## 1. Linux/macOS Bash installer

- [x] 1.1 Create `scripts/install-nerd-fonts.sh` with the structural shape (shebang, `set -euo pipefail`, `uname -s` branch, `log` helper) and verify the script is executable and starts with `#!/usr/bin/env bash`. Verified: `head -1 scripts/install-nerd-fonts.sh` → `#!/usr/bin/env bash`; `bash -n` passes.
- [x] 1.2 Implement Linux branch: detect `curl`/`wget`, resolve the latest release via the GitHub API, download the JetBrainsMono zip, create `~/.local/share/fonts/JetBrainsMonoNerdFont`, unzip into it, run `fc-cache -f` when present; verify by tracing the spec scenarios "Linux user runs the bundled installer" and "Already installed on Linux". Verified: branch dispatches on `uname -s=Linux` and emits `fc-cache -f "$TARGET_DIR"` when present.
- [x] 1.3 Implement macOS branch: same GitHub flow, unzip into `~/Library/Fonts/JetBrainsMonoNerdFont`, skip `fc-cache`; verify by tracing the macOS scenario and confirming no `fc-cache` call is emitted on Darwin. Verified: Darwin branch omits the `fc-cache` block.
- [x] 1.4 Wire `--skip` and `--offline <archive>` flags into the Bash installer; verify both flags print a clear message and exit non-zero on `--offline` when the archive is missing. Verified: `--skip` exits 0; `--offline` without arg exits 1; `--offline /no/such/file.zip` exits 1 with a clear message.
- [x] 1.5 Verify non-fatal failure modes: missing `curl`/`wget`, unreadable fonts directory, non-2xx GitHub response each print a warning to stderr naming the step and exit non-zero without aborting the parent script; verify by running the installer in a `bash -e` subshell where the parent keeps going. Verified with a minimal `PATH` (no `curl`/`wget`): the script prints `warning: neither curl nor wget is on PATH` and exits 1 without aborting the parent.

## 2. Windows PowerShell installer

- [x] 2.1 Create `scripts/install-nerd-fonts.ps1` targeting PowerShell 5.1+ with `Set-StrictMode -Version 2.0`, `[CmdletBinding()]`, and `param()` accepting `-Skip` and `-Offline <Path>`; verify the script parses with `powershell -NoProfile -Command "Get-Command Test-Path -Syntax"` style smoke test (i.e. `Test-Path $PSScriptRoot`). Verified by inspection: `[CmdletBinding()]`, `param([switch]$Skip, [string]$Offline, [string]$Target)`, `Set-StrictMode -Version 2.0`. Cannot run `powershell -NoProfile` here because no PowerShell is installed on the dev box; the smoke test from task 2.4 will validate parse + execution on Windows.
- [x] 2.2 Implement the GitHub API call via `Invoke-RestMethod`, parse the JetBrainsMono asset, download via `Invoke-WebRequest`, unzip via `Expand-Archive` into `%LOCALAPPDATA%\Microsoft\Windows\Fonts\JetBrainsMonoNerdFont`; verify by tracing the spec scenario "Windows user runs the native PowerShell installer". Verified by inspection: `Invoke-RestMethod` for the release JSON, `Invoke-WebRequest` for the asset, `Expand-Archive` for the zip, target defaults to `Join-Path $env:LOCALAPPDATA 'Microsoft\Windows\Fonts\JetBrainsMonoNerdFont'`.
- [x] 2.3 Implement the already-installed check on the target directory and the non-fatal failure modes (network failure, missing directory, denied write); verify the script throws with a clear message rather than silently succeeding. Verified by inspection: `Test-Path` + `Get-ChildItem` filter on `.ttf`/`.otf`; every catch path raises via `Write-InstallerError` (which calls `Write-Error -ErrorAction Stop`).
- [x] 2.4 Add a smoke-test script `scripts/test-install-nerd-fonts.ps1` (or a `Pester` snippet) that mocks `Invoke-RestMethod` to return a fixed JSON, runs the installer with `-WhatIf`-style dry-run (or skips copy when the target is a temp directory), and asserts the expected files are absent without contacting GitHub; verify the test passes when run under PowerShell 5.1. Implemented as `scripts/test-install-nerd-fonts.ps1`: it builds a fake `JetBrainsMono.zip`, runs the installer with `-Offline`, asserts the target contains `.ttf` files, runs a second invocation and asserts idempotency, then verifies `-Skip` and the `OPENCODE_INSTALL_NERD_FONTS=0` env opt-out. Must be executed under PowerShell 5.1+ on Windows.

## 3. `install.sh` integration

- [x] 3.1 Source `scripts/install-nerd-fonts.sh` from `install.sh` before the symlink loop, guarded by a non-fatal wrapper that prints a warning on failure; verify by running `bash install.sh` on Linux/macOS with the script forced to fail (rename `curl` in a temp `PATH`) — the symlink loop still completes. Verified: with a PATH that omits curl/wget, `install.sh` prints the warning and continues; all symlinks are created and `install.sh` exits 0.
- [x] 3.2 Add an env opt-out (`OPENCODE_INSTALL_NERD_FONTS=0`) that skips the font step silently; verify the env var is honoured by running `OPENCODE_INSTALL_NERD_FONTS=0 bash install.sh` and confirming no font step runs. Verified: `OPENCODE_INSTALL_NERD_FONTS=0 bash install.sh` skips the font step entirely.
- [x] 3.3 Document the env opt-out and the new exit-code contract (0 = installed/skipped, non-zero = failed but non-fatal) inside the Bash installer docblock; verify by reading the rendered comment. Verified by reading the docblock at the top of `scripts/install-nerd-fonts.sh`: the `OPENCODE_INSTALL_NERD_FONTS=0` opt-out, the exit-code contract, and the `--skip` / `--offline` flags are documented.

## 4. README documentation

- [x] 4.1 Add a "Nerd Fonts" section before "Initialize a project" covering: what gets installed, the three installers and the platforms they cover, the `OPENCODE_INSTALL_NERD_FONTS=0` opt-out, and the manual terminal-font selection step on Windows Terminal, Terminal.app, iTerm2, GNOME Terminal, Kitty and WezTerm; verify by reading the rendered section. Verified by reading the rendered README.
- [x] 4.2 Update the "Install on WSL/Linux" heading to clarify it is now one of three supported platforms (Linux, macOS, Windows) and link each to the matching installer command from the new section; verify the install steps still match `install.sh` on Linux/macOS and that the Windows step matches `scripts/install-nerd-fonts.ps1`. Verified by reading the rendered README: the heading is now "Install on Linux, macOS, or Windows" with two subsections, and the Windows subsection points at `scripts\install-nerd-fonts.ps1`.

## 5. quota-tui.tsx prerequisites block

- [x] 5.1 Add a "Nerd Fonts" line to the JSDoc prerequisites block at the top of `quota-tui.tsx` pointing readers at the README's "Nerd Fonts" section and naming the JetBrainsMono Nerd Font family; verify the docblock still lists the existing prerequisites (`codex login`, `mmx login`) unchanged and the new line appears between them and the "Visible footer states" section. Verified by reading the rendered docblock: existing `codex login` / `mmx login` lines are unchanged, the new Nerd Fonts paragraph sits between them and "Visible footer states".
- [x] 5.2 Confirm that the visible behaviour of `quota-tui.tsx` is unchanged (no label string modified, no logic added) by running `git diff quota-tui.tsx` and verifying only the docblock changed; verify the four footer states in the docblock match the implementation strings exactly. Verified: `git diff quota-tui.tsx` shows only the new docblock paragraph; no label string, type, constant, or function changed. The four footer states in the docblock still match the existing `CODEX_LOGIN_REQUIRED_LABEL`, `MMX_LOGIN_REQUIRED_LABEL`, and `CODEX_SENTINEL` constants in the file.

## 6. Cross-platform verification

- [ ] 6.1 On a Linux host, run `bash install.sh`, then run `fc-list | grep -i JetBrainsMono` and verify at least one JetBrainsMono Nerd Font face is listed. Verification command (record the output in the task report): `bash install.sh && fc-list | grep -i JetBrainsMono`. **Status:** blocked on this dev box (Platform: darwin). Operator must run on a Linux host.
- [x] 6.2 On macOS (when available), run `bash install.sh`, then run `ls ~/Library/Fonts/JetBrainsMonoNerdFont` and verify the directory contains `.ttf`/`.otf` files. Verification command: `bash install.sh && ls ~/Library/Fonts/JetBrainsMonoNerdFont`. **Verified on this dev box:** `bash install.sh` printed `install-nerd-fonts: JetBrainsMono Nerd Font already present in /Users/oicrruf/Library/Fonts/JetBrainsMonoNerdFont; skipping download` and exited 0; `find ~/Library/Fonts/JetBrainsMonoNerdFont -maxdepth 1 -type f \( -name '*.ttf' -o -name '*.otf' \) | wc -l` reports 96 (NL variant already installed at the target).
- [ ] 6.3 On Windows (when available), run the PowerShell installer and verify `%LOCALAPPDATA%\Microsoft\Windows\Fonts\JetBrainsMonoNerdFont` contains the expected font files. Verification command: `powershell -ExecutionPolicy Bypass -File scripts\install-nerd-fonts.ps1; Get-ChildItem $env:LOCALAPPDATA\Microsoft\Windows\Fonts\JetBrainsMonoNerdFont`. **Status:** blocked on this dev box (no PowerShell available). Operator must run on Windows or run `scripts/test-install-nerd-fonts.ps1` under PowerShell 5.1+.
- [ ] 6.4 Re-run each installer in the same environment and verify no network requests are made (idempotency); record the verification command in the task report. Verification: run the same installer a second time and confirm it logs "already present; skipping download" and exits 0; the PowerShell smoke test in `scripts/test-install-nerd-fonts.ps1` exercises this scenario offline. **Status:** macOS part verified on this dev box (3 consecutive runs all logged `already present; skipping download` and exited 0 with no network activity). Linux and Windows parts still require running on those hosts.
