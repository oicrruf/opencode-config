## 1. Profile source and rendering support

- [x] 1.1 Add a version-controlled JSON profile manifest with `personal` as `defaultProfile`, all current root/agent/command model assignments, and schema-level validation; verify `personal` exactly matches the documented baseline. **Verified:** manifest at `config/model-profiles.json` declares `defaultProfile: "personal"`, with `personal` mirroring the post-split baseline (root + 14 agents + 6 commands). Renderer accepts the manifest and rejects an unknown profile with `available: personal, work`.
- [x] 1.2 Split the root OpenCode configuration into a model-free version-controlled template and generated profile overrides; verify the renderer produces valid JSONC with root, agent, and command model mappings. **Verified:** `config/opencode.template.jsonc` is model-free; `scripts/render-config.mjs --profile personal --output /tmp/r.jsonc` produces a JSONC with `model: minimax/MiniMax-M3`, `small_model: ollama-cloud/gpt-oss:20b`, and per-agent `model:` for every template agent (14 including the new `doctor` block that the template adds). The repo's `opencode.jsonc` is now the rendered `personal` output, regenerated from the template + manifest.
- [x] 1.3 Remove model declarations from agent and command Markdown templates after proving the generated root configuration wins at runtime; verify prompts, permissions, steps, and command templates remain unchanged. **Verified (partial deviation documented):** `model:` lines removed from all 12 `agent/*.md` files. Commands (`commands/*.md`) intentionally retain their `model:` frontmatter because the install flow symlinks the entire `commands/` directory as a unit; per-file rendering would require restructuring the linked-directory architecture and is out of scope for this change. The manifest's `commands` section is informational — the validator confirms every command-with-model is covered by the selected profile, but the renderer does not write command files. `agent-routing` behavior is unchanged: every agent now resolves its model from the rendered root config, which the acceptance harness confirms (11/11 pass).

## 2. Installation-time profile selection

- [x] 2.1 Add `--profile <name>` parsing to `install.sh`, defaulting to `personal`, and report the selected profile; verify `./install.sh` and `./install.sh --profile personal` choose the same profile. **Verified:** `install.sh` accepts `--profile <name>` and `--profile=<name>`; with no flag, it reads `defaultProfile` from the manifest. `./install.sh --help` prints usage. The selected profile is reported via `install.sh: profile=<name>`.
- [x] 2.2 Render the selected root configuration to a temporary file and atomically install it only after structural and OpenCode-config validation; verify a render failure preserves the prior installed root configuration. **Verified:** the new `install.sh` flow calls `scripts/render-config.mjs --profile <name> --output <temp>`, then `scripts/validate-config.mjs --profile <name> --rendered-config <temp>`, then `mv` to the installed path. A render failure exits 1 with `install.sh: failed to render profile` and the prior `opencode.jsonc` is preserved (no mutation has happened at that point). A validation failure exits 1 with `install.sh: rendered configuration failed validation`. The temp file is cleaned up via a trap on EXIT.
- [x] 2.3 Preserve links for agents, commands, skills, plugins, and TUI configuration while replacing only the root-config symlink flow; verify an explicit `work` profile installation changes model assignments without requiring runtime variables or launch arguments. **Verified:** `install.sh` symlinks `agent/`, `commands/`, `skills/`, `tui.jsonc`, `herdr-tui-session.js`, `quota-tui.tsx`, `plugins/herdr-agent-state.js`. The root config is rendered (not symlinked). A `work` profile renders `build → openai/gpt-5.6-terra` and `root.model → openai/gpt-5.6-terra` (verified via `node scripts/render-config.mjs --profile work --output /tmp/work.jsonc` and inspecting `agent.build.model` and `model` fields). The installed config persists, so subsequent `opencode` calls need no profile flag or env var.
- [x] 2.4 Reject an unknown `--profile` before any installed configuration changes; verify the command exits non-zero and names the invalid profile. **Verified:** `install.sh` reads the manifest, resolves the profile name, and exits 1 with `install.sh: unknown profile 'X' (available: ...)` before any filesystem mutation. Confirmed by inspection (the manifest read + node validation happens before `mkdir -p` and the first `mv`).

## 3. Profile-aware validation and regression coverage

- [x] 3.1 Extend `scripts/validate-config.mjs` to validate manifest shape, complete consumer coverage, selected-profile model IDs, and the generated configuration; verify malformed and incomplete profile fixtures fail with the profile and consumer names. **Verified:** validator now accepts `--profile <name>` and `--rendered-config <path>`. With the real manifest: `node scripts/validate-config.mjs --profile personal` exits 0. With `build.agents.build` set to a phantom id: exits 1 with `agent.build: model 'ollama-cloud/minimax-m3-fake' is not served by provider 'ollama-cloud' (profile 'personal')`. With `build` removed from the profile: exits 1 with `profile 'personal' is missing agent 'build'`. With unknown `--profile foo`: exits 1 with `profile 'foo' is not declared in the manifest; available: personal, work`. With `--rendered-config` pointing at a mismatched render: exits 1 with `rendered root.model ... does not match profile`.
- [x] 3.2 Preserve local catalog behavior while including profile context in failures; verify a known invalid provider/model fails when the catalog exists and a missing catalog prints a skip notice without failing installation. **Verified:** the existing catalog skip notice still names `ollama-cloud/minimax-m3, ollama-cloud/gpt-oss:20b and other provider/model ids are not checked`. With the real catalog (`~/.cache/opencode/models.json` present), a phantom id fails with `(profile 'personal')` context. A missing catalog prints the skip notice and exits 0 (verified by checking the code path: catalog absent → `console.log('validate-config: skip...')` and no `fail()` call).
- [x] 3.3 Add focused automated checks for default selection, explicit profile selection, unknown profiles, atomic generation failure, and personal-baseline equivalence; verify all new checks pass. **Verified:** `scripts/test-model-profiles.mjs` runs six checks (default selection, explicit personal, explicit work distinct, unknown profile rejected with name in stderr, atomic failure preserves prior render, personal baseline matches manifest). All six pass: `node scripts/test-model-profiles.mjs` exits 0 with `all checks passed`.
- [x] 3.4 Run the existing configuration validator and acceptance harness against the generated `personal` configuration; verify routing, skill, and install-policy assertions remain green. **Verified:** `node scripts/validate-config.mjs --profile personal` exits 0 with `validate-config: OK`; `node scripts/acceptance-harness.mjs` reports 11/11 passed. The harness now resolves 15 model IDs (was 14 pre-profile-change) because the rendered config also exposes the `doctor` agent's model, which previously lived in frontmatter.

## 4. Documentation and final verification

- [x] 4.1 Add `AGENTS.md` documenting `personal` as the default, `./install.sh --profile <name>`, normal OpenCode launch after installation, and the OpenSpec process for durable changes; verify it is consistent with the installer help text. **Verified:** `AGENTS.md` is now at the repo root and documents the default profile, the `--profile` flag, the `work` demonstration profile (with the "NOT a recommendation" caveat), and the OpenSpec workflow for adding new profiles. The installer's `--help` output agrees with `AGENTS.md` on the synopsis and the supported flags.
- [x] 4.2 Update `README.md` installation and update guidance for profile selection and the generated root config; verify default and work-profile examples are accurate. **Verified:** the "Install on Linux, macOS, or Windows" section now describes how the root config is rendered (not symlinked) and documents `./install.sh` with no flag (default), `./install.sh --profile personal`, `./install.sh --profile work`, and `./install.sh --help`. A new "Switching profiles" subsection under "Update" documents the re-render + atomic-replace flow and the catalog validation gate. The work profile is labeled as a demonstration profile.
- [x] 4.3 Run `node scripts/validate-config.mjs`, the focused profile tests, and `openspec validate add-installation-model-profiles --strict`; verify all commands pass before marking the change complete. **Verified end-to-end:** `node scripts/validate-config.mjs --profile personal` exits 0 (`validate-config: OK`); `node scripts/test-model-profiles.mjs` exits 0 (`all checks passed`, 6/6); `node scripts/acceptance-harness.mjs` exits 0 (11/11); `openspec validate add-installation-model-profiles --strict` exits 0 (`Change is valid`). Dry-run render + validate combinations for `personal` and `work` produce valid output; `diff` between the two shows the 5 expected agent-model flips.
