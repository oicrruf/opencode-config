---
description: Git conventions — atomic commits, branch hygiene, revert strategy, history rewriting, conflict resolution (hidden skill, user-invoked)
agent: build
---

The following is the canonical `git-workflow` skill, injected from its
installed path. Follow it exactly; do not rely on a copy embedded here.

!`cat ~/.config/opencode/skills/git-workflow/SKILL.md`
