---
description: Router index of the user-invoked skills and when to reach for each (hidden skill, user-invoked)
agent: build
---

The following is the canonical `router` skill, injected from its installed
path. Follow it exactly; do not rely on a copy embedded here.

!`cat ~/.config/opencode/skills/router/SKILL.md`
